export const suma = (num1, num2) => {
    return num1 + num2;
}

const resta = (num1, num2) => {
    return num1 - num2; 
}

const multiplicacion = (num1, num2) => {
    return num1 * num2;
}

export const saludo = "HOLA, soy Manuel saludos desde un módulo";